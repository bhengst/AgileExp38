import { connection, currentUserID } from '../index'
function displayReservations() {

    // Room type
    // Room location
    // Resort
    // Days booked
    
    var table = document.getElementById("reserver-header");

    for (var i = 0; i < 2; i++) {
        var row = document.createElement("tr");

        for (var j = 0; j < 2; j++) {
            var cell = document.createElement("td");
            // var cellText = 
        }
    }
}